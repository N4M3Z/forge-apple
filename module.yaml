name: forge-apple
version: 0.3.0
description: Apple platform integration — Calendar, Reminders, Safari, Music (macOS). Daily overview via /Demo skill.
events: []

provides:
  email: false
  calendar: true
  reminders: true
  safari: true
  music: true

# External dependencies — not bundled, must be installed separately.
tools:
  ekctl: "EventKit CLI — required for Calendar + Reminders (see INSTALL.md)"
